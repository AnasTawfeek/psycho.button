import React, { Component } from 'react';
import styles from './styles.scss';

class PsychoButton extends Component {
    constructor(props) {
        super(props);
    }

    componentDidMount() {

    }

    render() {
        return (
            <button className="PsychoButton">
                Hola
            </button>
        )
    }
}

export default PsychoButton;
